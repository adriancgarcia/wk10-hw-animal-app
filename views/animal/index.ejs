<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head.ejs') %>
<body>
    <%- include('../partials/header.ejs') %>
    <main>
      <div>
        <h2>User: <%= user %></h2>
        <h2>List of Animals: </h2>
        <% for (animal of animals) { %>
          <article>
            <a href="/animals/<%= animal._id %>">
              <h2> <%= animal.name %></h2></a>
            <a href="/animals/<%=animal._id %>">
              <img src="<%= animal.image %>" alt="animal image"></a>
          </article>  
        <% } %>
        <h3><a href="/animals/new">Add new animal</a></h3>
        <a href="/user/logout"><button>Logout</button></a>
      <div>
    </main>
</body>
</html>